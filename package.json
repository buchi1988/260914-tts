{
  "name": "gemini-tts-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Gemini TTS web app running on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
